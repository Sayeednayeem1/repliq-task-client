import React from 'react';

const UsersList = () => {
    return (
        <div>
            {/* get the users by using GET operation in the backend */}
            <h1>This are the list of the users</h1>
        </div>
    );
};

export default UsersList;